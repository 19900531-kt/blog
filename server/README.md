# Blog GraphQL Server

actix-webとasync-graphqlを使用したGraphQLサーバーです。

## 起動方法

```bash
cargo run
```

サーバーは `http://127.0.0.1:8000/api/graphql` で起動します。

## GraphQL Playground

ブラウザで `http://127.0.0.1:8000/api/graphql` にアクセスしてGraphQLクエリを実行できます。


